<view class="container2">
    <view >
         <block wx:for="{{nodes}}">
              <view class="node-item">
                  <navigator url="zixun_xinxi?id={{item.id}}" hover-class="navigator-hover" >
                  <text class="node-title">{{item.title}}</text>
                  </navigator>
              </view>
         </block>
    </view>

    <loading hidden="{{hidden}}">
      加载中...
    </loading>
</view>